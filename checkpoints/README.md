# Checkpoints

Place the VPGNet checkpoint here before evaluation:

```text
checkpoints/vpgnet_epoch31_best.pth
```

The file is about 203 MiB, so it is not tracked in git by default. For GitHub,
upload it as a Release asset or use Git LFS.

Expected SHA256:

```text
5f6fad2ed4949df44eaba6aaae90d3dfe97b0630dd16d7bc35c3da9ea52f8d34
```

